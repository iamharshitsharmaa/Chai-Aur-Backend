db coonection first approach
------------------------------
import mongoose from "mongoose";
import { DB_NAME } from "./constants.js";
import express from "express";

const app = express();

;(async ()=>{
    try {
        mongoose.connect(`${process.env.MONGODUB_URL}/${DB_NAME}`)
        app.on("errorr", (error)=>{
            console.log("Error while connecting to MongoDB", error);
            throw error;
        })

        app.listen(process.env.port, ()=>{
            console.log(`Server is running on port ${process.env.port}`);
        })


    } catch (error) {
         console.log("Error while connecting to MongoDB", error);    
         throw error;('         throw error;')
    }
})()

SECOND APPROACH:-
on secoond approach we create index file in seperate db folder not all the code pushed on main index.js or lodder file this make our code look clean and easy to understand
using now in project
